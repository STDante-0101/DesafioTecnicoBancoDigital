{
  "ConnectionStrings": {
    "DefaultConnection": ""
    // ⚙️ CONFIGURAÇÃO DO BANCO DE DADOS:
    // 
    // Opção 1 (PADRÃO): InMemory Database
    // Deixe vazio: "DefaultConnection": ""
    // ✅ Não precisa instalar nada
    // ✅ Perfeito para desenvolvimento rápido
    // ⚠️  Dados perdidos ao reiniciar
    //
    // Opção 2: PostgreSQL Local
    // Descomente a linha abaixo:
    // "DefaultConnection": "Host=localhost;Port=5432;Database=bancodb;Username=bancoapi;Password=banco@2025"
    // ✅ Dados persistentes
    // ⚠️  Requer PostgreSQL instalado localmente
    //
    // Opção 3: Docker (use docker-compose.yml)
    // Não precisa configurar nada aqui!
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Information",
      "Microsoft.EntityFrameworkCore": "Information"
    }
  }
}